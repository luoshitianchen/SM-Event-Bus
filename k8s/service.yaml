apiVersion: v1
kind: Service
metadata:
  name: sm-event-bus
  namespace: sm-platform
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8300
      targetPort: http
  selector:
    app: sm-event-bus
